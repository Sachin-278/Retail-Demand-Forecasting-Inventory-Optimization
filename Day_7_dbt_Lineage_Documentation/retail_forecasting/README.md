# Day 7 — dbt Lineage & Documentation

Add the files in this package to the existing `retail_forecasting` dbt project.

Run:

```bash
dbt docs generate
dbt docs serve
```

This package documents the intended lineage. Actual documentation generation
must be run in the configured dbt/BigQuery environment.
